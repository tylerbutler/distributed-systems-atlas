---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { structureGroup, structureGroups } from "../../lib/structure-demo/structure-navigation";
---

<BaseLayout
  title="Structures"
  description="Learn how shared data can handle changes from several people."
  sheetTitle="Structures"
>
  <header class="page-intro">
    <h1>Learn through structures</h1>
    <p>
      Start with counters, then follow the families in order. Each lesson adds
      one new problem and shows how the structure handles it.
    </p>
    <a class="atlas-entry" href="/structures/models/">
      Compare CRDT, DDS, and OT structures
    </a>
  </header>

  <nav class="structure-guide" aria-label="Choose by need">
    <h2>Not sure where to start?</h2>
    <p>A changing total? <a href={structureGroup("counters").href}>Compare counters</a>.
      Membership with removals? <a href={structureGroup("sets").href}>Compare sets</a>.
      Several answers to one question? <a href={structureGroup("registers").href}>Compare registers</a>.</p>
  </nav>

  <ol class="structure-index" aria-label="Structure lessons">
    {structureGroups.map((group, index) => (
      <li><a href={group.href}>
        <h2>{group.title}</h2>
        <p>{group.summary}</p>
        {index === 0 && <span class="status">Start here</span>}
      </a></li>
    ))}
  </ol>
</BaseLayout>

<style>
  .page-intro,
  .structure-guide,
  .structure-index {
    max-width: var(--measure-reading);
    margin-inline: auto;
  }

  .structure-guide {
    padding-block: var(--space-4);
    border-block: var(--rule-width) solid var(--line);
  }

  .structure-guide h2,
  .structure-guide p {
    margin-block: 0 var(--space-2);
  }

  .structure-index {
    padding: 0;
    list-style: none;
    counter-reset: structures;
  }

  .structure-index li {
    border-top: var(--rule-width) solid var(--line);
    counter-increment: structures;
  }

  .structure-index li > a {
    display: grid;
    grid-template-columns: minmax(10rem, 0.7fr) minmax(0, 1.3fr);
    gap: var(--space-4);
    min-height: 44px;
    align-items: baseline;
    padding-block: var(--space-5);
    color: inherit;
    text-decoration-color: var(--line);
  }

  .structure-index h2,
  .structure-index p {
    margin: 0;
  }

  .structure-index .status {
    grid-column: 1 / -1;
  }

  .structure-index h2::before {
    content: counter(structures) ". ";
    color: var(--muted);
    font-family: var(--font-data);
    font-size: 0.875rem;
  }

  @media (width < 48rem) {
    .structure-index li > a {
      grid-template-columns: 1fr;
      gap: var(--space-2);
    }
  }
</style>
