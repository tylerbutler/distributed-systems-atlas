<section class="landing-signal instrument-panel" aria-labelledby="landing-title">
  <header class="landing-premise">
    <h1 id="landing-title">Start with a structure you already know</h1>
    <p>
      Start with counters, then move to richer shared state. Open the
      reference atlas when you need a specific mechanism or failure mode.
    </p>
  </header>

  <div class="starting-path">
    <h2><a href="/structures/counters/">Counters</a></h2>
    <p>
      Begin with additions that merge without a winner. Then add corrections,
      sequencing, removal, and conflict rules as each lesson needs them.
    </p>
  </div>

  <div class="landing-actions">
    <a class="trail-entry" href="/structures/counters/">Start with counters</a>
    <a class="atlas-entry" href="/atlas/">Open the reference atlas</a>
  </div>
</section>

<style>
  .landing-signal {
    margin: 0;
    padding: var(--space-6);
    border: 0;
  }

  .landing-premise {
    display: grid;
    grid-template-columns: minmax(0, 1.2fr) minmax(18rem, 0.8fr);
    gap: var(--space-6);
    align-items: end;
    padding-bottom: var(--space-6);
    border-bottom: var(--rule-width) solid var(--clear);
  }

  h1 {
    max-width: 16ch;
    margin: 0;
    font-size: clamp(2.25rem, 4.6vw, 4rem);
  }

  .landing-premise p {
    max-width: 42ch;
    margin: 0;
    color: var(--clear);
  }

  .starting-path {
    max-width: 44rem;
    padding-block: var(--space-6);
    border-bottom: var(--rule-width) solid var(--clear);
  }

  .starting-path h2 {
    margin: 0;
    color: var(--clear);
    font-size: clamp(1.8rem, 3vw, 2.5rem);
  }

  .starting-path h2 a {
    color: inherit;
  }

  .starting-path > p:last-child {
    margin: var(--space-3) 0 0;
    color: var(--clear);
  }

  .landing-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-3) var(--space-6);
    padding-top: var(--space-5);
  }

  .trail-entry {
    padding: 0.75rem 1rem;
    color: var(--instrument-deep);
    background: var(--signal);
    font-family: var(--font-ui);
    font-weight: 650;
    text-align: center;
    text-decoration-thickness: 1px;
  }

  .trail-entry:hover {
    background: var(--clear);
    text-decoration-thickness: 2px;
  }

  @media (width < 64rem) {
    .landing-premise {
      grid-template-columns: 1fr;
      align-items: start;
    }

  }

  @media (width < 40rem) {
    .landing-signal {
      padding: var(--space-5);
    }

    h1 {
      font-size: clamp(2rem, 9vw, 3rem);
    }

    .landing-actions {
      flex-direction: column;
      align-items: stretch;
      gap: var(--space-1);
      text-align: center;
    }
  }
</style>
