name: distributed-systems-atlas
version: 1.0.0
description: APM project for distributed-systems-atlas
author: Tyler Butler
# Which agent platforms to deploy to.
# Resolution order: --target flag > this field > auto-detect from filesystem.
# Accepted values: agent-skills, antigravity, claude, codex, copilot, cursor, gemini, grok-build, hermes, kiro, opencode, vscode, windsurf
targets:
  - claude
  - copilot
dependencies:
  apm: []
  mcp: []
includes: auto
scripts: {}
devDependencies:
  apm:
    - pbakaus/impeccable/plugin#skill-v4.1.2
    - tylerbutler/apm-base
